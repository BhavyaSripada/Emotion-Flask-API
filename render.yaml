services:
  - type: web
    name: my-python-app
    env: python
    runtime: python3.11.4 # Use the closest available version if 3.11.4 is not supported
    buildCommand: |
      pip install -r requirements.txt
    startCommand: python main.py
  
